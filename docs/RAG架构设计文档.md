# RAG架构设计文档

本文档详细记录了 MindScribe（灵辑）系统的检索增强生成（RAG）架构设计，包括当前的实现逻辑和未来的演进规划。

## 一、 现有设计：结构优先的精确切片（Structure-First RAG）

当前系统采用“结构优先”的策略，利用 Markdown 文档的层级结构进行精确切片。

### 1.1 核心逻辑

系统将 Markdown 文档视为一个层级化的树状结构，利用标题标记（`#` 和 `##`）作为天然的语义边界。

1.  **意图识别与参数提取**：
    *   LLM 识别用户意图（如 `SUMMARY`）。
    *   提取关键参数：`doc_title`（文档标题）和 `target_chapter`（目标章节）。
    *   例如：用户问“第三章讲了什么”，提取 `target_chapter="第三章"`。

2.  **结构化切片（Structure-Based Slicing）**：
    *   **定位**：使用正则表达式（如 `^## 第X章`）在文档中定位章节的起始位置。
    *   **边界识别**：向下扫描寻找下一个同级标题（`##`）或父级标题（`#`）作为结束位置。
    *   **提取**：提取起始和结束位置之间的所有文本作为上下文。

3.  **全文兜底（Full-Text Fallback）**：
    *   如果用户未指定章节，或文档没有章节结构，系统回退到全文处理模式。
    *   直接将整个文档内容作为上下文发送给 LLM。

### 1.2 优缺点分析

*   **优点**：
    *   **高精度**：提取的内容具有完美的语义边界，不会截断句子或段落。
    *   **低成本**：不需要向量数据库，不需要 Embedding 计算，纯文本处理速度快。
    *   **可解释性强**：检索逻辑清晰直观，易于调试。

*   **缺点**：
    *   **依赖规范**：高度依赖文档的 Markdown 结构。如果文档是“流水账”或无结构笔记，切片逻辑失效。
    *   **长文档限制**：对于无结构的超长文档，全文兜底可能触发 LLM 的 Token 限制。

---

## 二、 未来设计：混合增强策略（Hybrid RAG Strategy）

针对无结构文档（如零散笔记、数学题集）的场景，未来将采用 **“写入时重组” + “语义向量检索”** 的组合策略。

### 2.1 方案一：写入时重组（The "Librarian" Approach）—— 强化现有的智能归类

**核心理念**：既然用户不写章节，AI 就帮他写。在数据**写入**阶段就强制结构化，从而复用现有的“结构优先”检索优势。

*   **工作流程**：
    1.  **用户输入**：例如“这道微积分题很难...”
    2.  **语义分析**：系统后台分析这条笔记的语义（数学 -> 微积分）。
    3.  **自动归档**：系统自动在文档中寻找或创建 `## 微积分` 或 `## 错题集` 章节。
    4.  **结果**：原本零散的笔记被自动组织成了结构化的 Markdown。

*   **价值**：
    *   保持文档的可读性和有序性。
    *   无需修改现有的检索逻辑，直接复用结构化切片能力。
    *   文档越写越有序，形成良性循环。

*   **挑战**：
    *   需要处理用户坚持“流水账”记录习惯的情况，避免过度干预。

### 2.2 方案二：语义向量检索（The "Search Engine" Approach）—— 标准 RAG 引入

**核心理念**：承认文档的无序性，通过高维向量空间来寻找相关性。

*   **工作流程**：
    1.  **颗粒度切片**：不再按章节切，而是按“条目”或“段落”切分。
        *   例如：对于数学题，将“题干+解答”作为一个 Block。
    2.  **Embedding**：将每个 Block 转化为向量，存入轻量级向量库（如 Chroma 或本地 FAISS）。
    3.  **检索**：当用户问“上次那道关于极限的题”时，计算余弦相似度，提取最相关的 Top-K 个 Block。

*   **价值**：
    *   对“零散笔记”极其有效，能精准定位大海捞针的信息。
    *   解决了无结构文档无法精确检索的问题。

*   **挑战**：
    *   引入了向量库的复杂度。
    *   可能丢失上下文信息（如题目与前后复习计划的关联）。

### 2.3 核心技术模块详解 (NLP Module Specification)

在 F2K 评测体系的语境下，“引入新的 NLP 处理模块”通常指：从当前简单的“正则匹配 + 基础 LLM”（v1.0），升级为“向量检索 + 语义分析”（v2.0）。

*   **v1.0 (现状)**：靠关键词和 Markdown 符号找笔记（刚性，易失效）。
*   **v2.0 (未来)**：靠语义理解找笔记（柔性，更智能）。

NLP 处理模块具体包含以下核心组件，分别对应“写入”和“读取”两个阶段：

#### 1. 写入侧 NLP 模块：智能整理者 (The Organizer)
*目标：提升“分类准确率”和“语义概括度”指标。*

*   **语义分类器 (Semantic Classifier)**
    *   **功能**：分析用户输入的文本语义，将其自动映射到预定义的知识体系中。
    *   **例子**：用户输入“$ \int x^2 dx $”，分类器识别出这是“数学/微积分”，而不是“英语单词”。
    *   **技术实现**：目前可能依赖 LLM 的 Few-shot Prompting，未来可使用轻量级的 BERT 分类模型以降低延迟。

*   **摘要与标题生成器 (Summarizer & Title Generator)**
    *   **功能**：为一段零散的笔记生成一个简短、精准的标题或标签。
    *   **例子**：用户记录了一段关于会议的流水账，模块自动生成标题：“2025-12-30 产品评审会议纪要”。
    *   **技术实现**：基于生成式 LLM（如 GPT-4o-mini 或本地 Llama 3）。

*   **语义去重模块 (Semantic Deduplicator)**
    *   **功能**：判断新输入的笔记是否与库中已有的笔记在**意思上**重复，而不仅仅是文字重复。
    *   **例子**：已有“记得买牛奶”，新输入“下班带瓶奶”，模块识别为重复并建议合并。
    *   **技术实现**：计算文本的 Embedding 向量相似度。

#### 2. 读取侧 NLP 模块：智能检索者 (The Retriever)
*目标：提升“模糊查询召回率”和“多跳聚合准确率”指标。*

*   **Embedding 模型 (Vectorization)**
    *   **功能**：将文本（笔记或查询词）转化为高维向量（一串数字），捕捉其深层语义。
    *   **作用**：这是实现“模糊检索”的基础。它让系统知道“微积分”和“导数”在语义空间上是接近的。
    *   **技术实现**：使用 OpenAI Embedding API 或本地 Sentence-BERT 模型。

*   **重排序模型 (Reranker)**
    *   **功能**：对初步检索回来的 Top-N 个结果进行精细打分，将最相关的排在最前面。
    *   **作用**：解决向量检索有时候“由于语义太泛而找不准”的问题。
    *   **技术实现**：Cross-Encoder 模型。

*   **查询重写模块 (Query Rewriter)**
    *   **功能**：将用户模糊、口语化的查询转化为更适合检索的规范表达。
    *   **例子**：用户问“上次那个很难的题”，模块结合上下文重写为“查找最近标记为‘困难’的数学错题”。

### 2.4 实施路线图

1.  **短期（Smart Classification 2.0）**：
    *   利用现有的智能归类功能，尽可能引导内容结构化。
    *   优化 Prompt，提高归类的准确性和用户接受度。

2.  **长期（Local Vector Store）**：
    *   对于无法归类的“流水账”，在浏览器端或本地建立轻量级索引。
    *   实现混合检索：优先尝试结构化检索，失败后回退到向量检索。

---

## 三、 终极产品形态：混沌缓冲区 (The Chaos Box)

基于上述技术架构的演进，MindScribe 的最终完成目的是打造一个**“零压力”的知识输入与管理系统**。

### 3.1 竞品分析：现状与局限

我们分析了市场上主流的笔记与知识管理工具，发现它们在处理“碎片记录”时均存在不同程度的局限性：

| 竞品 | 方案 | 痛点/局限 |
| :--- | :--- | :--- |
| **Flomo (浮墨)** | **纯标签流**。鼓励用户手动打标签（#tag），通过标签来组织内容。 | **依赖自律**。如果用户懒得打标签，或者标签体系混乱（比如混用了 #读书 和 #阅读），后期检索就是灾难。 |
| **Apple Notes / 备忘录** | **快速备忘**。主打启动快，iCloud 同步。 | **数据孤岛**。笔记之间没有联系，写了就“死”在那里了，很难被再次利用。 |
| **Notion AI** | **侧重生成**。AI 主要用来扩写、润色、总结已有的长文档。 | **入口太重**。打开 Notion -> 找页面 -> 找位置 -> 打字，这个过程太长，不适合“闪念”。 |
| **Mem.ai** | **自动关联**。主打 "Self-organizing workspace"，通过 AI 自动关联相似笔记。 | **黑盒操作**。AI 默默把笔记关联起来，但用户不知道它怎么关联的，缺乏掌控感。且对中文支持一般。 |

### 3.2 我们的解决方案：The Chaos Box

针对上述痛点，MindScribe 2.0 将实现**“混沌缓冲区”**，作为 F2K (Fragment-to-Knowledge) 体系的最佳实践载体。

*   **核心痛点**：用户有时候只想快速记一句话，不想考虑它属于哪个文档、哪个章节，也不想手动打标签。
*   **功能设计**：
    1.  **极速入口 (Fast Entry)**：
        *   一个类似聊天窗口的轻量级输入框。
        *   用户只管往里扔文字、图片、语音，无需任何预处理。
    2.  **后台整理 (Background Processing)**：
        *   AI 在后台默默工作（基于 F2K 体系的写入侧 NLP 模块）。
        *   自动进行语义分类、摘要生成和去重检查。
    3.  **定期汇报 (Human-in-the-loop)**：
        *   系统定期（如每天/每周）向用户汇报整理结果。
        *   交互示例：“主人，您昨天扔进来的 5 条碎片，我建议归档到《工作日志》和《灵感集》中，您确认吗？”

*   **价值主张**：
    *   **彻底消除“开始记录”的心理压力**：用户只需负责“发射”，系统负责“命中”。
    *   **透明可控**：通过定期汇报机制，让用户保持对知识库的掌控感，避免“黑盒”带来的不信任。
