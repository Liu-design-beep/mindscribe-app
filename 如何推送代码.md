# 如何推送代码到 GitHub

## 🎯 最简单的方法：直接使用命令行

不需要任何脚本，只需要几个简单的命令。

---

## 📋 完整步骤

### 第1步：打开 CMD

```
按 Windows + R
输入 cmd
按 Enter
```

### 第2步：进入项目目录

```bash
cd /d "I:\简历\展示整理12.13\灵辑\程序 (2)"
```

### 第3步：推送代码

```bash
git push -u origin main
```

### 第4步：输入认证信息

如果提示需要认证：

```
Username: Liu-design-beep
Password: [输入您的 Personal Access Token]
```

**注意：密码要输入 Token，不是账户密码！**

---

## 🔑 如何获取 Personal Access Token

### 步骤1：访问 GitHub

1. 登录 https://github.com
2. 点击右上角头像 → Settings

### 步骤2：创建 Token

1. 左侧选择 "Developer settings"
2. 选择 "Personal access tokens"
3. 选择 "Tokens (classic)"
4. 点击 "Generate new token (classic)"

### 步骤3：配置 Token

1. **Note**: 输入 `Git Push`（任意名称）
2. **Expiration**: 选择 `No expiration` 或 `90 days`
3. **Select scopes**: 勾选 `repo`（全部仓库权限）
4. 点击 "Generate token"

### 步骤4：复制 Token

**重要：Token 只显示一次！请立即复制并保存！**

---

## ✅ 验证推送成功

推送完成后，访问：
https://github.com/Liu-design-beep/mindscribe-app

应该能看到您的代码文件。

---

## 🔄 以后每次推送

以后每次推送代码，只需要3个命令：

```bash
# 1. 进入项目目录
cd /d "I:\简历\展示整理12.13\灵辑\程序 (2)"

# 2. 添加并提交更改
git add .
git commit -m "Update: 修改说明"

# 3. 推送
git push
```

**就这么简单！**

---

## 📝 常用命令速查

| 命令 | 作用 |
|------|------|
| `git status` | 查看当前状态 |
| `git add .` | 添加所有文件 |
| `git commit -m "..."` | 提交代码 |
| `git push` | 推送到 GitHub |
| `git log` | 查看提交历史 |

---

## ⚠️ 常见问题

### 问题1：提示 "Connection was reset"

**原因**：网络问题或认证失败

**解决**：
- 检查网络连接
- 确保使用 Token 而不是密码
- 可以尝试使用 SSH 方式（见下方）

### 问题2：提示 "Authentication failed"

**原因**：用户名或密码错误

**解决**：
- 确保用户名正确：`Liu-design-beep`
- 确保使用 Token，不是账户密码
- 检查 Token 是否有 `repo` 权限

### 问题3：远程仓库已有内容

**解决**：先拉取，再推送

```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

---

## 🔐 可选：使用 SSH 方式（更稳定）

如果 HTTPS 方式有问题，可以使用 SSH：

### 1. 生成 SSH 密钥

```bash
ssh-keygen -t ed25519 -C "your.email@example.com"
```
（直接按回车使用默认设置）

### 2. 查看公钥

```bash
type %USERPROFILE%\.ssh\id_ed25519.pub
```

### 3. 添加到 GitHub

1. 访问：https://github.com/settings/keys
2. 点击 "New SSH key"
3. 粘贴公钥并保存

### 4. 切换为 SSH

```bash
git remote set-url origin git@github.com:Liu-design-beep/mindscribe-app.git
```

### 5. 推送

```bash
git push -u origin main
```

---

## 🎉 总结

**不需要任何脚本，只需要记住这几个命令：**

```bash
git add .
git commit -m "..."
git push
```

**就这么简单！**

