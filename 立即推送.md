# 立即推送代码到 GitHub

## ⚠️ 重要安全提示

您刚才提供的 Token 是敏感信息，**不应该公开分享**！

**建议：推送完成后，立即撤销这个 Token 并生成新的！**

---

## 🚀 推送步骤

### 第1步：打开 CMD（不是 PowerShell）

```
按 Windows + R
输入 cmd
按 Enter
```

### 第2步：进入项目目录

```bash
cd /d "I:\简历\展示整理12.13\灵辑\程序 (2)"
```

### 第3步：推送代码

```bash
git push -u origin main
```

### 第4步：输入认证信息

当提示输入用户名和密码时：

```
Username: Liu-design-beep
Password: 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
```

**注意：**
- 用户名输入：`Liu-design-beep`
- 密码输入：上面的完整 Token（包括 `github_pat_` 前缀）

---

## ✅ 验证推送成功

推送完成后，访问：
https://github.com/Liu-design-beep/mindscribe-app

应该能看到您的代码文件。

---

## 🔒 推送完成后立即撤销 Token

### 步骤1：访问 GitHub

1. 访问：https://github.com/settings/tokens
2. 找到刚才使用的 Token
3. 点击右侧的删除按钮（垃圾桶图标）
4. 确认删除

### 步骤2：生成新 Token

1. 点击 "Generate new token (classic)"
2. 勾选 `repo` 权限
3. 生成新 Token
4. **保存到安全的地方**（不要分享给任何人）

---

## 📝 如果推送失败

### 错误1：Authentication failed

- 检查用户名是否正确：`Liu-design-beep`
- 检查 Token 是否完整（包括 `github_pat_` 前缀）
- 确保 Token 有 `repo` 权限

### 错误2：Connection was reset

- 检查网络连接
- 可以尝试多次推送

### 错误3：远程仓库已有内容

执行：

```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

---

## 🎯 快速命令总结

```bash
# 1. 进入项目目录
cd /d "I:\简历\展示整理12.13\灵辑\程序 (2)"

# 2. 推送代码
git push -u origin main

# 3. 输入认证信息
# Username: Liu-design-beep
# Password: [粘贴 Token]
```

---

**现在就打开 CMD 执行推送吧！** 🚀

